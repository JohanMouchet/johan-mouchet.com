import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { select, text } from "@storybook/addon-knobs";
import Main from ".";

<Meta title="Partials/Main" component={Main} />

# Main

<Canvas>
  <Story name="default">
    <div style={{ outline: "1px dotted" }}>
      <Main
        margin={select("margin", {
          "": null,
          "no-margin": "no-margin",
        })}
      >
        {text("children", "children")}
      </Main>
    </div>
  </Story>
</Canvas>

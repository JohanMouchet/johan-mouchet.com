import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { select, text } from "@storybook/addon-knobs";
import Aside from ".";

<Meta title="Partials/Aside" component={Aside} />

# Aside

<Canvas>
  <Story name="default">
    <div style={{ outline: "1px dotted" }}>
      <Aside
        margin={select("margin", {
          "": null,
          "no-margin": "no-margin",
        })}
      >
        {text("children", "children")}
      </Aside>
    </div>
  </Story>
</Canvas>

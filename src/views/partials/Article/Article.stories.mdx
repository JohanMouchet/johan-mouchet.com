import { Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import { select, text } from "@storybook/addon-knobs";
import Article from ".";

<Meta title="Partials/Article" component={Article} />

# Article

<Canvas>
  <Story name="default">
    <div style={{ outline: "1px dotted" }}>
      <Article
        size={select("size", { "": null, sm: "sm", lg: "lg" })}
        margin={select("margin", {
          "": null,
          "no-margin": "no-margin",
          "no-top-margin": "no-top-margin",
          "no-bottom-margin": "no-bottom-margin",
        })}
        variant={select("variant", { "": null, secondary: "secondary" })}
      >
        {text("children", "children")}
      </Article>
    </div>
  </Story>
</Canvas>

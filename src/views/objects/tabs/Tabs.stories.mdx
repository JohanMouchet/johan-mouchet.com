import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text } from "@storybook/addon-knobs";
import Tabs from ".";

<Meta title="Objects/Tabs" component={Tabs} />

# Tabs

<Preview>
  <Story name="default">
    <Tabs
      tabs={[
        {
          thumb: text("thumb", "thumb 1", "Thumb 1"),
          panel: text("panel", "panel 1", "Thumb 1"),
        },
        {
          thumb: text("thumb", "thumb 2", "Thumb 2"),
          panel: text("panel", "panel 2", "Thumb 2"),
        },
      ]}
    />
  </Story>
</Preview>

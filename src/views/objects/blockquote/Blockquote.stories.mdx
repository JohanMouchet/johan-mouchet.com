import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text, boolean } from "@storybook/addon-knobs";
import Blockquote from ".";

<Meta title="Objects/Blockquote" component={Blockquote} />

# Blockquote

<Preview>
  <Story name="default">
    <Blockquote
      quote={text("quote", "quote")}
      cite={text("cite", "cite")}
      pulled={boolean("pulled", true)}
    />
  </Story>
</Preview>

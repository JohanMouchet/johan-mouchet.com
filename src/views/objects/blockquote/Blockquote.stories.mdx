import { Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import { boolean, text } from "@storybook/addon-knobs";
import Blockquote from ".";

<Meta title="Objects/Blockquote" component={Blockquote} />

# Blockquote

<Canvas>
  <Story name="default">
    <Blockquote
      quote={text("quote", "Quote")}
      cite={text("cite", "Cite")}
      pulled={boolean("pulled", false)}
    />
  </Story>
</Canvas>

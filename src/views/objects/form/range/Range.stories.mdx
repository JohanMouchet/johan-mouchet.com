import { Fragment } from "react";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { text } from "@storybook/addon-knobs";
import { Label } from "views/objects";
import Range from ".";

<Meta title="Objects|Form/Range" component={Range} />

# Range

<Preview>
  <Story name="default">
    {() => {
      const id = text("id", "id");
      return (
        <Fragment>
          <Label id={id} block>
            Range
          </Label>
          <Range id={id} name={text("name", "name")} />
        </Fragment>
      );
    }}
  </Story>
</Preview>

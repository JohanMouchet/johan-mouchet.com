import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text } from "@storybook/addon-knobs";
import { Textarea } from ".";

<Meta title="Objects/Form/Fields/Textarea" component={Textarea} />

# Textarea

<Preview>
  <Story name="default">
    <Textarea id={text("id", "id")} name={text("name", "name")}>
      {text("Text", "text")}
    </Textarea>
  </Story>
</Preview>

import { Fragment } from "react";
import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text } from "@storybook/addon-knobs";
import { Textarea } from ".";
import { Label } from "../layout";

<Meta title="Objects|Form/Fields" component={Textarea} />

# Textarea

<Preview>
  <Story name="Textarea">
    {() => {
      const id = text("id", "id");
      return (
        <Fragment>
          <Label id={id} block>
            Textarea
          </Label>
          <Textarea id={text("id", "id")} name={text("name", "name")}>
            {text("children", "children")}
          </Textarea>
        </Fragment>
      );
    }}
  </Story>
</Preview>

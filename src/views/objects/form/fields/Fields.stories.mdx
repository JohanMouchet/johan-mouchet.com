import { Fragment } from "react";
import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text, select } from "@storybook/addon-knobs";
import { Field } from ".";
import { Label } from "views/objects";

<Meta title="Objects|Form/Fields" component={Field} />

# Field

<Preview>
  <Story name="Field">
    {() => {
      const id = text("id", "id");
      return (
        <Fragment>
          <Label id={id} block>
            Field
          </Label>
          <Field
            id={text("id", "id")}
            type={select("type", { text: "text", search: "search" })}
            name={text("name", "name")}
            value={text("value", "Value")}
          />
        </Fragment>
      );
    }}
  </Story>
</Preview>

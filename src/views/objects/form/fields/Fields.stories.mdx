import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text, select } from "@storybook/addon-knobs";
import { Field } from ".";

<Meta title="Objects/Form/Fields/Field" component={Field} />

# Field

<Preview>
  <Story name="default">
    <Field
      id={text("id", "id")}
      type={select("type", { text: "text", search: "search" })}
      name={text("name", "name")}
      value={text("value", "")}
    />
  </Story>
</Preview>

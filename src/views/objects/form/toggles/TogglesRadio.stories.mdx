import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text, boolean } from "@storybook/addon-knobs";
import { Radio } from ".";
import Label from "../layout";

<Meta title="Objects/Form/Toggles" component={Radio} />

# Radio

<Preview>
  <Story name="Radio">
    {() => {
      const id = text("id", "id");
      const name = text("name", "name");
      const value = text("value", "value");
      const children = text("children", "Radio");
      return (
        <div>
          <Label id={id} block>
            Radio
          </Label>
          <Radio
            id={id}
            name={name}
            value={`${value}-1`}
            checked={boolean("checked", true)}
          >
            {`${children} 1`}
          </Radio>
          <Radio name={name} value={`${value}-2`}>
            {`${children} 2`}
          </Radio>
        </div>
      );
    }}
  </Story>
</Preview>

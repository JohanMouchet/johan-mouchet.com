import { Fragment } from "react";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { select, text } from "@storybook/addon-knobs";
import { Field } from "views/objects";
import { FormGroup, Label } from ".";

<Meta title="Objects|Form/Layout" component={FormGroup} />

# FormGroup

<Preview>
  <Story name="FormGroup">
    <FormGroup
      variant={select("variant", {
        "": null,
        success: "success",
        error: "error",
        warning: "warning",
      })}
      detail={text("detail", "Detail")}
    >
      <Fragment>
        <Label>Label</Label>
        <Field />
      </Fragment>
    </FormGroup>
  </Story>
</Preview>

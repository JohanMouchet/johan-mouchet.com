import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text } from "@storybook/addon-knobs";
import File from ".";

<Meta title="Objects/Form/File" component={File} />

# File

<Preview>
  <Story name="default">
    <File
      id={text("id", "id")}
      name={text("name", "name")}
      multipleCaption={text("multipleCaption", "Multiple Caption")}
      label={text("label", "")}
    >
      {text("children", "Choose file")}
    </File>
  </Story>
</Preview>

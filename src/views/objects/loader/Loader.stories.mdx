import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { select } from "@storybook/addon-knobs";
import Loader from ".";

<Meta title="Objects/Loader" component={Loader} />

# Loader

<Preview>
  <Story name="default">
    <Loader size={select("size", { "": null, sm: "sm" })} />
  </Story>
</Preview>

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { boolean, number, text } from "@storybook/addon-knobs";
import ProgressBar from ".";

<Meta title="Objects/ProgressBar" component={ProgressBar} />

# ProgressBar

<Canvas>
  <Story name="default">
    <ProgressBar
      label={text("label", "Label")}
      progress={text("progress", "Progress")}
      progressPct={boolean("progressPct")}
      total={text("total", "Total")}
      detailsOnHover={boolean("detailsOnHover")}
      filling={number("filling", 50, {
        range: true,
        min: 0,
        max: 100,
      })}
    />
  </Story>
</Canvas>

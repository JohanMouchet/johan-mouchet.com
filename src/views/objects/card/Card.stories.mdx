import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text, boolean, select } from "@storybook/addon-knobs";
import Card from ".";

<Meta title="Objects/Card" component={Card} />

# Card

<Preview>
  <Story name="default">
    <Card
      banner={boolean("banner", true)}
      bannerUrl={text("bannerUrl", "bannerUrl")}
      header={text("header", "header")}
      body={text("body", "body")}
      footer={text("footer", "footer")}
      variant={select("variant", {
        "": null,
        secondary: "secondary",
        primary: "primary",
        hover: "hover",
        forward: "forward",
        active: "active",
        "banner-gradient": "banner-gradient",
      })}
      size={select("size", {
        "": null,
        sm: "sm",
        lg: "lg",
      })}
    />
  </Story>
</Preview>

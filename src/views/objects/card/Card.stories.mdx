import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text, boolean, select } from "@storybook/addon-knobs";
import Card from ".";

<Meta title="Objects|Card" component={Card} />

# Card

<Preview>
  <Story name="default">
    <div className="grid">
      <div className="cell cell--4-@xs">
        <Card
          banner={boolean("banner", true)}
          bannerUrl={text("bannerUrl", "https://via.placeholder.com/400x168")}
          header={text("header", "Header")}
          footer={text("footer", "Footer")}
          variant={select("variant", {
            "": null,
            secondary: "secondary",
            primary: "primary",
            hover: "hover",
            forward: "forward",
            active: "active",
            "banner-gradient": "banner-gradient",
          })}
          size={select("size", {
            "": null,
            sm: "sm",
            lg: "lg",
          })}
        >
          {text("children", "children")}
        </Card>
      </div>
    </div>
  </Story>
</Preview>

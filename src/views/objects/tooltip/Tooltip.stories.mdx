import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { boolean, select, text } from "@storybook/addon-knobs";
import Tooltip from ".";

<Meta title="Objects|Tooltip" component={Tooltip} />

# Tooltip

<Preview>
  <Story name="default">
    <Tooltip
      content={text("content", "Content")}
      position={select("position", {
        "": null,
        top: "top",
        right: "right",
        bottom: "bottom",
        left: "left",
      })}
      indicator={boolean("indicator")}
      multiLines={boolean("multiLines")}
    >
      {text("children", "children (hover)")}
    </Tooltip>
  </Story>
</Preview>

import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text, select } from "@storybook/addon-knobs";
import { Button } from ".";

<Meta title="Objects/Button" component={Button} />

# Button

<Preview>
  <Story name="default">
    <Button
      type={select("type", {
        anchor: "anchor",
        button: "button",
        submit: "submit",
        reset: "reset",
      })}
      url={text("url", "url")}
      variant={select("variant", {
        variant: "variant",
        primary: "primary",
        secondary: "secondary",
      })}
      size={select("size", {
        sm: "sm",
        lg: "lg",
      })}
      block={select("block", {
        "@sm": "@sm",
        "@md": "@md",
      })}
    >
      {text("children", "children")}
    </Button>
  </Story>
</Preview>

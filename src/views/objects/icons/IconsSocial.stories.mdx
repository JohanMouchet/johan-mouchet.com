import { Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import {
  Icon500px,
  IconCodepen,
  IconDribbble,
  IconGithub,
  IconInstagram,
  IconLinkedIn,
  IconSpotify,
  IconTwitter,
  IconUnsplash,
  IconYoutube,
} from ".";

<Meta title="Objects/Icons/Social" component={Icon500px} />

# Icons - Social

<Canvas>
  <Story name="default">
    {() => {
      const icons = [
        <Icon500px />,
        <IconCodepen />,
        <IconDribbble />,
        <IconGithub />,
        <IconInstagram />,
        <IconLinkedIn />,
        <IconSpotify />,
        <IconTwitter />,
        <IconUnsplash />,
        <IconYoutube />,
      ];
      return (
        <div className="grid u-align--center">
          {icons.map((icon, index) => (
            <div
              className="cell cell--3-@xs cell--2-@sm cell--1-@md"
              key={index}
            >
              {icon}
            </div>
          ))}
        </div>
      );
    }}
  </Story>
</Canvas>

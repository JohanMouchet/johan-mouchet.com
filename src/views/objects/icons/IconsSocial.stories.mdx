import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import {
  Icon500px,
  IconCodepen,
  IconDribbble,
  IconGithub,
  IconInstagram,
  IconLinkedIn,
  IconSpotify,
  IconTwitter,
  IconUnsplash,
  IconYoutube,
} from ".";

<Meta title="Objects|Icons" component={Icon500px} />

# Icons - Social

<Preview>
  <Story name="Social">
    {() => {
      const icons = [
        <Icon500px />,
        <IconCodepen />,
        <IconDribbble />,
        <IconGithub />,
        <IconInstagram />,
        <IconLinkedIn />,
        <IconSpotify />,
        <IconTwitter />,
        <IconUnsplash />,
        <IconYoutube />,
      ];
      return (
        <div className="grid u-align--center">
          {icons.map((icon) => (
            <div className="cell cell--3-@xs cell--2-@sm cell--1-@md">
              {icon}
            </div>
          ))}
        </div>
      );
    }}
  </Story>
</Preview>

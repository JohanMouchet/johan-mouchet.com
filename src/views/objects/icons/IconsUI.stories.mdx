import { Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import { IconChevronDown, IconSearch } from ".";

<Meta title="Objects/Icons/UI" component={IconChevronDown} />

# Icons - UI

<Canvas>
  <Story name="default">
    {() => {
      const icons = [<IconChevronDown />, <IconSearch />];
      return (
        <div className="grid u-align--center">
          {icons.map((icon, index) => (
            <div
              className="cell cell--3-@xs cell--2-@sm cell--1-@md"
              key={index}
            >
              {icon}
            </div>
          ))}
        </div>
      );
    }}
  </Story>
</Canvas>

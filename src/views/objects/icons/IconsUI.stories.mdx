import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { IconChevronDown, IconSearch } from ".";

<Meta title="Objects|Icons" component={IconChevronDown} />

# Icons - UI

<Preview>
  <Story name="UI">
    {() => {
      const icons = [<IconChevronDown />, <IconSearch />];
      return (
        <div className="grid u-align--center">
          {icons.map((icon) => (
            <div className="cell cell--3-@xs cell--2-@sm cell--1-@md">
              {icon}
            </div>
          ))}
        </div>
      );
    }}
  </Story>
</Preview>

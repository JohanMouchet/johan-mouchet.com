import { Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import {
  IconArrowReturn,
  IconCheckCircle,
  IconChevronDown,
  IconCloudUpload,
  IconExclamationCircle,
  IconExclamationTriangle,
  IconInfoCircle,
  IconSearch,
  IconX,
} from ".";
import "./Icons.stories.scss";

<Meta title="Objects/Icons/UI" component={IconChevronDown} />

# Icons - UI

<Canvas>
  <Story name="default">
    {() => {
      const icons = [
        <IconArrowReturn />,
        <IconCheckCircle />,
        <IconChevronDown />,
        <IconCloudUpload />,
        <IconExclamationCircle />,
        <IconExclamationTriangle />,
        <IconInfoCircle />,
        <IconSearch />,
        <IconX />,
      ];
      return (
        <div className="grid u-align--center grid--noGutter-@xs">
          {icons.map((icon, index) => (
            <div className="cell storybook-icon" key={index}>
              {icon}
            </div>
          ))}
        </div>
      );
    }}
  </Story>
</Canvas>

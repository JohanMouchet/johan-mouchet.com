import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text } from "@storybook/addon-knobs";
import Anchor from ".";

<Meta title="Objects|Anchor" component={Anchor} />

# Anchor

<Preview>
  <Story name="default">
    <Anchor
      as={text("as", "h2")}
      id={text("id", "id")}
      marker={text("marker", "#")}
    >
      {text("children", "Anchor (hover)")}
    </Anchor>
  </Story>
</Preview>

import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text, select } from "@storybook/addon-knobs";
import Details from ".";

<Meta title="Objects/Details" component={Details} />

# Details

<Preview>
  <Story name="default">
    <Details
      summary={text("summary", "summary")}
      variant={select("variant", { compact: "compact" })}
    />
  </Story>
</Preview>

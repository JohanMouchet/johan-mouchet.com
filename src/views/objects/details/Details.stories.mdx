import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { select, text } from "@storybook/addon-knobs";
import Details from ".";

<Meta title="Objects/Details" component={Details} />

# Details

<Canvas>
  <Story name="default">
    <Details
      summary={text("summary", "Summary")}
      variant={select("variant", { "": null, compact: "compact" })}
    >
      {text("children", "children")}
    </Details>
  </Story>
</Canvas>

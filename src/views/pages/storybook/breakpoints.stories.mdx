import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { BREAKPOINTS } from "constants/breakpoints";

<Meta title="Settings|Breakpoints" />

# Breakpoints

<Preview>
  <Story name="default">
    <table className="o-table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Size</th>
          <th>Sass Setting</th>
        </tr>
      </thead>
      <tbody>
        {[
          {
            name: "xs",
            size: BREAKPOINTS.XS,
          },
          {
            name: "sm",
            size: BREAKPOINTS.SM,
          },
          {
            name: "md",
            size: BREAKPOINTS.MD,
          },
          {
            name: "lg",
            size: BREAKPOINTS.LG,
          },
          {
            name: "xl",
            size: BREAKPOINTS.XL,
          },
        ].map((breakpoint) => (
          <tr>
            <th>{breakpoint.name}</th>
            <td>{breakpoint.size}</td>
            <td>
              <code className="u-select--all">
                map-get($s-breakpoints, {breakpoint.name})
              </code>
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </Story>
</Preview>

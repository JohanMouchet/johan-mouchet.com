import { Canvas, Meta, Story } from "@storybook/addon-docs/blocks";
import { Table } from "views/objects";

<Meta title="Styles/Spacing" />

# Spacing

<Canvas>
  <Story name="default">
    <Table>
      <thead>
        <tr>
          <th>Size</th>
          <th>Preview</th>
          <th>Sass Tool</th>
        </tr>
      </thead>
      <tbody>
        {[1, 2, 3, 4, 5, 6].map((spacing) => (
          <tr key={spacing}>
            <th>{spacing}</th>
            <td>
              <div
                className={`storybook-spacing storybook-spacing--${spacing.toString()}`}
              ></div>
            </td>
            <td>
              <code className="u-select--all">t-modular-scale({spacing})</code>
            </td>
          </tr>
        ))}
      </tbody>
    </Table>
  </Story>
</Canvas>

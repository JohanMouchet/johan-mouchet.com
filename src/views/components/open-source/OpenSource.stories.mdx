import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { text } from "@storybook/addon-knobs";
import { Elements } from "prismic-reactjs";
import OpenSource from ".";

<Meta title="Components/OpenSource" component={OpenSource} />

# OpenSource

<Canvas>
  <Story name="default">
    <OpenSource
      projects={[
        {
          link: {
            url: text("url", "#"),
          },
          name: text("name", "Name"),
          lede: [
            {
              type: Elements.paragraph,
              text: text("lede", "Lede"),
              spans: [],
            },
          ],
          description: [
            {
              type: Elements.listItem,
              text: text("description", "Description"),
              spans: [],
            },
          ],
        },
      ]}
    />
  </Story>
</Canvas>

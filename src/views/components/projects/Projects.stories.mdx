import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { boolean, text } from "@storybook/addon-knobs";
import { Elements } from "prismic-reactjs";
import Projects from ".";

<Meta title="Components/Projects" component={Projects} />

# Projects

<Canvas>
  <Story name="default">
    <Projects
      projects={[
        {
          highlight: boolean("highlight", true),
          name: text("name", "Name"),
          tagline: text("tagline", "Tagline"),
          link: {
            url: text("url", "#"),
          },
          lede: [
            {
              type: Elements.paragraph,
              text: text("lede", "Lede"),
              spans: [],
            },
          ],
          thumbnailSrc: {
            url: text("thumbnailSrc", "https://via.placeholder.com/400x225"),
          },
          achievements: [
            {
              type: Elements.listItem,
              text: text("achievements", "Achievement"),
              spans: [],
            },
          ],
          architecture: [
            {
              type: Elements.paragraph,
              text: text(
                "architecture",
                "Architecture 1, Architecture 2, Architecture 3"
              ),
              spans: [],
            },
          ],
          libraries: [
            {
              type: Elements.paragraph,
              text: text("libraries", "Library 1, Library 2, Library 3"),
              spans: [],
            },
          ],
        },
      ]}
    />
  </Story>
</Canvas>

import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text, boolean, array } from "@storybook/addon-knobs";
import Projects from ".";

<Meta title="Components/Projects" component={Projects} />

# Projects

<Preview>
  <Story name="default">
    <Projects
      projects={[
        {
          highlight: boolean("highlight", true),
          name: text("name", "name"),
          tagline: text("tagline", "tagline"),
          url: text("url", "url"),
          lede: text("lede", "lede"),
          thumbnailSrc: text("thumbnailSrc", "thumbnailSrc"),
          achievements: array("achievements", [
            "achievements",
            "achievements",
            "achievements",
          ]),
          architecture: array("architecture", [
            "architecture",
            "architecture",
            "architecture",
          ]),
          libraries: array("libraries", [
            "libraries",
            "libraries",
            "libraries",
          ]),
        },
      ]}
    />
  </Story>
</Preview>

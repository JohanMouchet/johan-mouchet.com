import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { boolean, date, text } from "@storybook/addon-knobs";
import { Elements } from "prismic-reactjs";
import Experiences from ".";

<Meta title="Components|Experiences" component={Experiences} />

# Experiences

<Preview>
  <Story name="default">
    <Experiences
      experiences={[
        {
          companyLink: {
            url: text("companyLink", "#"),
          },
          companyName: text("companyName", "Name"),
          location: text("location", "Location"),
          jobTitle: text("jobTitle", "Job Title"),
          startDate: new Date(date("startDate", new Date())),
          present: boolean("present", false),
          endDate: new Date(date("endDate", new Date())),
          contractType: text("contractType", "Contract Type"),
          lede: [
            {
              type: Elements.paragraph,
              text: text("lede", "Lede"),
              spans: [],
            },
          ],
        },
      ]}
    />
  </Story>
</Preview>

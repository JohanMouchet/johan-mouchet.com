import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { text, boolean, select } from "@storybook/addon-knobs";
import PersonalWorks from ".";
import { IconUnsplash, IconYoutube } from "views/objects";

<Meta title="Components/PersonalWorks" component={PersonalWorks} />

# PersonalWorks

<!-- TDOD: is sourceIcon an Array? -->

<Preview>
  <Story name="default">
    <PersonalWorks
      works={[
        {
          video: boolean("video", true),
          url: text("url", "url"),
          thumbnailPath: text("thumbnailPath", "thumbnailPath"),
          sourceIcon: select("sourceIcon", {
            Unsplash: <IconUnsplash />,
            Youtube: <IconYoutube />,
          }),
        },
      ]}
    />
  </Story>
</Preview>
